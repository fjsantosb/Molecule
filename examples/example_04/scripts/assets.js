Molecule.module('assets', function (game) {

    return {
    
        audio: {
        
            // Load audio with parameter: an array of paths
            castle: game.audio.load(['assets/castle.mp3'])
            
        }
        
    }
    
});